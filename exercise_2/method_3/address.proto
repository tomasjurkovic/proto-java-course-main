syntax = "proto3";

// this is created using imports in files

import "building.proto";
import "city.proto";
import "street.proto";

message Address {
    City city = 1;
    Street street = 2;
    Building building = 3;
}